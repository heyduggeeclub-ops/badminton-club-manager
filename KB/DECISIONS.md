# DECISIONS.md — 重要決策紀錄

> 記錄「為什麼這樣做」的架構與產品決策，避免日後重複討論或誤改。
> 規則本身（怎麼做）記錄在 `AGENTS.md`「開發慣例」，本檔只補充決策脈絡／原因，不重複列出規則內容。

## 已知決策（摘自現有文件，脈絡多數待確認）

| 決策 | 內容 | 規則記錄位置 | 原因／脈絡 |
|---|---|---|---|
| 資料異動走 Server Actions | 不在 client 端直連 Supabase 寫入 | `AGENTS.md` 開發慣例 | 待確認（現有文件未說明原因，推測為安全與一致性考量） |
| 手機版優先 | 改 UI 時先驗證窄螢幕，BottomNav 為主要導航 | `AGENTS.md` 開發慣例 | 待確認是否對應 PRD「保留 LINE 接龍習慣」的手機使用情境 |
| 歷史收費紀錄不受新規則影響 | 新收費規則以 `effective_from` 生效日期區隔新舊規則 | PRD、`fee_rules` 資料表設計 | PRD 明確要求，非推測 |
| 臨打（非會員）沿用 `members.role` 欄位，不新增獨立欄位 | 2026-07-11：新增臨打功能時，直接讓 `role` 多一個 `'guest'` 值，並比照既有 `leader_fee`／`vice_leader_fee`「角色固定費用」模式，在 `fee_rules` 加上 `guest_fee_male`／`guest_fee_female` | `supabase/migrations/014_guest_fee.sql`、`get_fee_amount()` | `role` 欄位原本語意是「幹部職位」，嚴格說跟「是否為會員」是兩個不同概念；但專案既有的角色固定費用機制（get_fee_amount 依 p_role 判斷）剛好就是為了處理「這個角色不走出席次數階梯，固定收費」的情境，臨打的需求完全符合這個模式。為了不新增一個平行的欄位／機制、降低程式複雜度，選擇重用 `role`。**待確認**：未來若臨打人數變多，需要更精細的臨打管理（例如記錄邀請人、單次來源等），屆時可能需要重新評估是否拆出獨立欄位或資料表 |
| 本季出席次數校正採「單一會員逐一設定」，不做整季批次匯入介面 | 2026-07-11：討論季度中途導入系統的校正需求時，曾提出「會員詳情頁逐一填」與「設定頁整季批次填」兩種方案，選擇前者 | `member_season_adjustments` 表、`members/[id]/page.tsx` | 使用者評估球隊每次出席人數不多，萬一發生「季度中途導入系統」這種情況，逐一到會員詳情頁設定就夠用，不需要額外做批次介面增加複雜度。**待確認**：若未來換一個出席人數多很多的社團使用，可能要重新評估是否需要批次匯入 |

## 使用方式

未來若有新的重要決策（技術選型、架構取捨、產品規則調整等），請在此新增一列，並註明日期與原因；不要只記錄在對話或平台記憶中。
